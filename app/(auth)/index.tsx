import { Layout, Spinner, Text } from "@ui-kitten/components";

export default function IndexCreen() {
    return(
        <Layout style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
            <Spinner />
        </Layout>
    )
}
